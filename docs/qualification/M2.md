# M2 Linux Docker dependency/ddmin qualification

Status: **PASS**, bounded to the exact source, CI environment, immutable images,
and checked-in fixtures recorded below.

## Identity

| Subject | Exact value |
|---|---|
| Repository | `taipei49314/tomorrowci-lab` |
| Source/default SHA | `456a36edb1e8547612cd13ee7a30be3479d33bab` |
| CI | [run 31316809823](https://github.com/taipei49314/tomorrowci-lab/actions/runs/31316809823), success |
| Job | [`m2-dependency-fixtures` / 93253469760](https://github.com/taipei49314/tomorrowci-lab/actions/runs/31316809823/job/93253469760), success |
| Artifact | `tomorrowci-m2-dependency-evidence`; ID `9039044133`; digest `sha256:402c5085be7890fa57b930f6ea9744b28d5e351b78ef0e78857ad365ac0fd2f3` |
| Artifact expiry | 2026-11-07 |
| Qualified environment | GitHub-hosted Linux x86_64 runner, Docker |

## Observed fixture results

Each fixture contains two candidate changes: `breaking-api` and
`irrelevant-noise`. Native package-manager probes and TomorrowCI both observed
the baseline as passing, the full candidate as failing, `breaking-api` alone as
failing with a stable signature, and `irrelevant-noise` alone as passing. This
exhausts all non-empty subsets for these two-change fixtures and establishes
observed minimality only for this bounded case.

| Ecosystem | Manager / image | Run ID | Scan result | Downloaded-bundle read-back |
|---|---|---|---|---|
| Python 3.11 | pip; `python@sha256:90744cff8f32887f075c47d747a173ff333e9e98801667af93c357fa9f5e28ff` | `04054e94457a` | dependency frontier observed; `ddmin-breaking-api` minimal; replay x2 PASS | current-v2 verify PASS, 140 files |
| Node 20 | npm; `node@sha256:8f693eaa7e0a8e71560c9a82b55fd54c2ae920a2ba5d2cde28bac7d1c01c9ba5` | `c8781bdadb33` | dependency frontier observed; `ddmin-breaking-api` minimal; replay x2 PASS | current-v2 verify PASS, 140 files |
| Rust 1.83 | cargo; `rust@sha256:a45bf1f5d9af0a23b26703b3500d70af1abff7f984a7abef5a104b42c02a292b` | `45c8b2fa6b57` | dependency frontier observed; `ddmin-breaking-api` minimal; replay x2 PASS | current-v2 verify PASS, 140 files |

The failing full candidate and minimal subset were each classified from three
stable failing attempts. Exact manifest hashes, before/after content hashes,
resolved versions, resolution hashes, attempt outputs, failure hashes, and
subtraction observations are retained in the CI artifact.

## Independent download/read-back

The artifact was downloaded with `gh run download 31316809823` into a new
directory. The current CLI then verified all three run IDs as current-v2 with
`checked_files=140`. The three scan logs contained their expected run IDs, and
all six replay logs reported replay PASS with matching exit and normalized
failure signature. This detects archive/download corruption within the stated
checks, but it remains a project-operated read-back rather than independent
external evidence.

## Boundary

The following remain **NOT_RUN**: the M3 general Node and Rust runtime matrix,
Podman, Windows/macOS and broader Linux platform qualification, arbitrary
third-party dependency graphs, the interactive report, remote URL scans, and
container image publication. Independent maintainer/auditor adoption remains
externally blocked. No release or broad support claim follows from this M2
PASS.
