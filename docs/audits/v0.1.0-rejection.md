# v0.1.0 rejection summary

**Tag:** `v0.1.0` (preserved for auditability — do not delete or rewrite)  
**Commit:** `7a08c4884761b70e9ae0e63012ee87fdc1e39348`  
**Public repo at claim time:** `taipei49314/tomorrowci-lab`  
**Audit:** `TOMORROWCI_GROK45_FORENSIC_AUDIT.md` (2026-08-06)  
**Verdict:** **REJECT** — not a valid public v0.1.0 release candidate (score 38/100)

## Why it was rejected

The repository is a substantial pre-alpha scaffold, not an empty mockup. Independent forensic review rejected completion claims because the **central product promise was not established**:

> No forecast without an executable scenario. No breakage claim without replayable evidence.

Primary defects (see full audit for file-level detail):

| ID | Defect | Audit verdict |
|----|--------|---------------|
| P0-1 | Public CI red while README marked M0–M5 Done | FAIL |
| P0-2 | Acceptance path uses `ScriptedExecutor` / gen-demo, not live Docker | FAIL |
| P0-3 | Fetch and test in separate disposable containers destroy install state | FAIL |
| P0-4 | Image digest / fetch failures swallowed (`.ok().flatten()`, `let _ =`) | FAIL |
| P0-5 | Fixtures do not prove advertised axes (dep fixture simulated, etc.) | FAIL |
| P0-6 | ddmin / adaptive search not end-to-end | FAIL |
| P0-7 | Replay does not enforce digest, fetch, signature match | FAIL |
| P0-8 | Action never dogfooded (`uses: ./action` absent); consumer build broken | FAIL |
| P0-9 | Required React/TypeScript interactive report does not exist | FAIL |
| P0-10 | Release integrity incomplete (empty SBOM, no CI gate, no image) | FAIL |

## Automatic disqualifiers (mission)

- Dashboard/demo uses scripted outcomes (`sha256:scripted-test-digest`)
- Container tags without required immutable digests
- Live tests created but not executed while milestones marked Done
- CI never invokes the checked-in Action
- Release completion claimed without required container image
- Package-manager support claimed beyond real execution (e.g. `uv`)

## What remains true

- Multi-crate Rust architecture and typed verdict model
- Sandbox safe-default intent (no privileged, no docker.sock, disposable copy)
- Multi-OS release artifact *production* (Windows PE verified in audit)
- Narrow XSS helper tests pass
- Scripted tests prove **classifier/orchestration semantics only**

## Repair track

Branch: `repair/real-python-vertical-slice`  
Candidate (only after acceptance): `v0.1.1-alpha.1`  
Scope: one **live** Python runtime-break vertical slice + honest claims + CI/Action dogfood.

Do not treat this document as rewriting history. Tag `v0.1.0` stays as evidence of the rejected candidate.
